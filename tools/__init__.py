"""Compatibility package for legacy ``tools.*`` imports.

The project source lives under :mod:`src.tools`; some integration tests
and older scripts still import top-level ``tools`` modules. Keep this
thin forwarding package until those callers are migrated.
"""

